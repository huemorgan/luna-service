# 01 — Home renders + Products nav

**Goal:** The marketing home renders the full story and the Products menu links to all three product pages.

## Steps
1. Open `/` (logged out).
2. Screenshot the hero. Read the DOM.
3. Click **Products ▾** in the header.
4. Click each of: Luna Open Source, Luna Hosting Service, Luna Marketplaces.

## Pass criteria
- Hero shows H1 "Agents you can actually rely on" (with gradient italic accent) + a **Start free** primary button.
- Home contains, in order: hero, the problem (first-gen "Claw" chaos vs Luna), the two pillars (Reliability + Trust), how-it-works (plugin scaffold / vault / rollback / deterministic workflow), self-improving-made-safe, use cases, trust strip, products triptych, pricing teaser, closing CTA band with the "no limits … trust and reliability" line.
- Products ▾ opens a glass menu with 3 entries, each with a gradient visual (no icons), and each routes to the correct page.
- No console errors; images load (not broken).
